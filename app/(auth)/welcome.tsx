import { View, Text } from "react-native";
import { SafeAreaView } from "react-native-safe-area-context";

export default function AuthLayout() {
	return (
		<SafeAreaView>
			<View>
				<Text style={{ color: "white" }}>Textadf</Text>
			</View>
		</SafeAreaView>
	);
}
